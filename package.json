{
  "name": "qrgenie-qr-decoder",
  "version": "1.0.0",
  "private": true,
  "description": "Browser extension (Chrome, Firefox, Edge) that decodes QR codes on screen, entirely on your device.",
  "scripts": {
    "test": "node --test test/*.test.js",
    "check": "node --check background.js && node --check common/payload.js && node --check common/crop.js && node --check common/preprocess.js && node --check content/overlay.js && node --check content/area-select.js && node --check popup/popup.js && node --check viewer/viewer.js && node --check scripts/make-icons.js && node --check scripts/make-fixtures.js && node --check scripts/build.js",
    "build": "node scripts/build.js",
    "make:icons": "node scripts/make-icons.js",
    "make:fixtures": "node scripts/make-fixtures.js"
  }
}
